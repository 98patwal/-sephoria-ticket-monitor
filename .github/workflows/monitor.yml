name: SEPHORiA Ticket Monitor

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check resale inventory
        run: |
          echo "Checking Weezevent..."
          curl -s "https://api.weezevent.com/ticket/widgets/resale-sephoria-london-2026?locale=en-gb"
